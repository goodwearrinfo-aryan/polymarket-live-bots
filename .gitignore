# Runtime state / logs / secrets — never store in the cloud repo
__pycache__/
*.pyc
*.log
*_state.json
*.pid
run_output.txt
.smtp_creds.json
.env
.last_*
.alert_*
.fade_gate
.zero_fetch
.scalp_engine_launch
alerts.log
.DS_Store
polymarket_status.pdf
polymarket_bot_report.pdf
# keep: all *.py, *.md, *.service, *.sh, *.command, ml/dataset.csv, bot_backtest.json
# runtime DBs, backups, extra state markers
*.db
*.bak
*.bak.*
*.bak_*
.fade_forward_start
.forward_cutoff
moves_log.jsonl
*.jsonl
# bot-generated runtime snapshots (rewritten every watchdog cycle)
market_heatmap.json
trade_log.json
trader_stats.json
# paper-book live state (accumulates every cycle; tracking it dirties git +
# clobber-races parallel sessions — 2026-06-15). basket_paper_book.json etc.
*_book.json
# bot brain state + signal_verifier runtime outputs
brain.json
.signal_seen.json
latest_signal.txt
verified_signals.jsonl
rejected_signals.jsonl
new_trades_*.pdf
# win-rate tracking removed (censored /positions) + new quality-boost runtime
trader_win_rate.json
winrate_leaderboard.json
wallet_signal_pnl.json
.feed_tmp/
phone_feed.log
.rr_fix_at
# runtime caches / state archives / local backups / generated report PDFs
scalp_lab_cache.json
scalp_lab_state_archive_*.json
_backup_pre_db/
backups/
pnl_report_*.pdf
scalp_lab_report.pdf
pnl_baseline.json
.pandascore_key
.solscan_key
.ta_env
log_archive/moves_log*
*.bak-*
graphify-out/
*.out
oss-bots/

# per-cycle ephemeral churn — regenerated every watchdog cycle; real state lives in
# Postgres (db.py) + the Obsidian mirror, so git-tracking these is pure noise (2026-06-16)
ws_prices.json
.oddpool_cycle
.watchdog_mtime
.insider_rejected.json
scalp_lab_dashboard.html
*_cache.json
*_signals.json

# 2.2M gamma full-sweep cache (regenerated each run by gamma_sweep.py)
gamma_markets_full.json

# launchd-health sentinel runtime state
launchd_health.log
.launchd_health_seen.json

# dataarb leg runtime state
dataarb_state.json
datagate_arbs.json
dataarb.log

# live-map lights (activity_light / connector_light / wiring_keeper) runtime stamps + state
.connector_last
.wiring_keeper_last
.connector_state.json
.wiring_keeper_state.json
.activity_graph_state.json

# generated dashboard + insider/kalshi runtime state (untracked 2026-06-21; files kept on disk)
agents_dashboard.html
insider_seen.json
insider_finder_log.json
kalshi_markets.json

# per-cycle runtime state + auto-generated probe findings (untracked 2026-06-22; files kept on
# disk; real state lives in Postgres + the Obsidian mirror, so git-tracking these is pure churn)
.pm_digest_lastcrit
.sentinel_seen.json
.whalecopy_INCONCLUSIVE
arb_memory.json
backtest_results.json
backtest_summary.json
defender_verdicts.json
pm_autonomy_ledger.json
stock_agents_brief.json
consensus_findings.md
macro_findings.md
orderflow_findings.md
tech_findings.md
trending_findings.md
log_archive/*.gz
.remote_imsg_seen.json
com.aryan.brain-bloom.plist
.world_read_state.json
brains.py

# 2026-07-03: iCloud sync-conflict duplicate copies ("file 2.ext") — incl. ".env 2",
# a conflict copy of the secrets file. Never commit these; clean up manually.
* 2.*
* 3.*
.env*
# vendored third-party toolboxes (BrainSpace, 88MB) — re-fetchable, not ours
vendor/
# runtime one-line markers / locks / scratch (regenerated by the 24/7 jobs)
.gate_30_alerted
.insider_finder.lock
.keyed_last
.whalecopy_GRADUATED
err.tmp
live_tape.json
vol_regime_latest.json
* 2
* 3
whale_copy_state.json
whale_copy_paper.log
sports_settle_arb.log
edge_judge.log
*.log.*.bak
__pycache__/
sports_settle_arb.log.*
