# Polymarket 6h Routine — 2026-06-11 22:40 IST (manual test run)

## ⚠️ FAILURES FIRST

1. **`leg_health.py` CRASHES** — `ValueError: target_mid: unknown leg 'fgcrypto'` in `honest_report.py:40`. A leg named `fgcrypto` exists in state but was never registered in `target_mid()`. Health reporting for all legs after `lateprox` (incl. nearres, nearresfade, the 10 new legs) is **silently missing** until fixed.
2. **`crypto_tracking.log` is STALE — last write Jun 8 12:16, 3+ days old.** The crypto-tracking scheduled task reports "lastRunAt today" but is not writing to this log. coinup/coindown/diverg/feargreed/lateprox still show 0 priced exits — data accumulation may be stalled.

## 1. Health check
- launchd: watchdog, pnl-alert, brain, backtest, daily-report, obsidian all loaded (status 0). `polymarket-lab-pdf` last exit **1** (nonzero), `com.aryan.jesse` last exit **1**.
- watchdog_loop.sh alive, running `scalp_lab.py once` (correct mode). Canonical `polymarket_bot.py --dry-run` running since Wed.
- 1 known stray bot copy running from the agent-sessions outputs dir (left alive per heal-not-kill policy).

## 2. P&L + legs (from state mirror, pnl_usdc, no-data excluded)
| leg | exits | WR | total P&L | mean/exit | 95% CI |
|---|---|---|---|---|---|
| **nearres** | 24 | 20/24 (83%) | **+$0.93** | +0.0388 | [+0.012, +0.065] ✅ excludes 0 |
| nearresfade | 13 (+6 open) | 8/13 | +$0.31 | +0.0239 | [-0.050, +0.094] noise |
| allin (control) | 450 | 28% | −$68.02 | −0.151 | [−0.176, −0.127] losing ✅ |
| coinflip (control) | 250 | 39% | −$25.08 | −0.100 | [−0.131, −0.071] losing ✅ |
| coindown/coinup (control/leg) | 0 | — | — | — | no priced exits yet |

Controls are losing with CI excluding 0 → marking looks honest.

## 3. Edge graduation gate
- **nearres: 24/30 exits. CI > 0. Controls losing. 6 exits from graduating.** But open=0 — no new entries currently; check entry filter isn't starved.
- nearresfade: 13/30 exits, CI includes 0. Early.
- fade_checkpoint.py still points at dead truefade (per project notes) — pending re-point decision.

## 4. Data freshness
- gamma-api: 200 OK (0.22s) ✅  clob: 200 OK (0.24s) ✅
- scalp_lab_state.json + scalp_lab_cache.json: written 22:34 (minutes ago) ✅
- crypto_tracking.log: **STALE (Jun 8)** ❌ — see failure #2.
